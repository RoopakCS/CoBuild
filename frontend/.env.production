# Production — points to deployed backend
# Replace with the actual Render/Railway backend URL from M1
# This can also be set as an Environment Variable in Vercel dashboard (preferred)
VITE_API_URL=https://cobuild-backend.onrender.com

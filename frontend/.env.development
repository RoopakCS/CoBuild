# Local development — Vite proxy handles /api requests, no URL needed
# The dev proxy in vite.config.js forwards /api/* to localhost:8080
# VITE_API_URL is intentionally unset so axios.js falls back to '/api'
